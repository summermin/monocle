- if current_user
  = link_to 'Sign Out', destroy_user_session_path, method: :delete, class: 'btn grey sign-out'
- else
  = link_to 'Sign Up', new_user_registration_path, class: 'btn green sign-up'
  = link_to 'Sign In', new_user_session_path, class: 'btn grey sign-in'